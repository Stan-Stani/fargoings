name = "wfpl-feed"
main = "worker.js"
compatibility_date = "2026-05-15"
workers_dev = true

# Secret (NOT stored here): WFPL_KEY — set with `npx wrangler secret put WFPL_KEY`.
# Must equal the ?key= value in the VPS /root/fargoings/.env WFPL_ICS_URL.
